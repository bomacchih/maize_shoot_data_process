# save images in R
# Open a PNG device
png("plot_image.png", width = 800, height = 600)

# Create the plot
plot(1:10, main = "Sample Plot", xlab = "X-axis", ylab = "Y-axis")

# Close the device
dev.off()
